import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  entry__title: {
    fontSize: '2.5rem',
  },
  entry__actions: {
    marginTop: '50px',
    textAlign: 'center',
  },
}));

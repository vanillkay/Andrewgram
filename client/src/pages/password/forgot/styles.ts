import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  'forgot-page': {
    width: '100%',
    height: '50vh',
    fontSize: '1.5rem',
    paddingTop: '4rem',
  },
}));
